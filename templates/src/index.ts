// Entry point template - each project creates its own Root.tsx and Composition.tsx
// This file is copied into each new project directory by the AI

import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
